import React, { Component } from 'react';
import Utilisation from '../utilisation/index';

class Utile extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <div>
        <Utilisation />
      </div>
    );
  }
}

export default Utile;
